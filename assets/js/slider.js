document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.slider').forEach(slider=>{let down=false,startX,scroll;slider.addEventListener('mousedown',e=>{down=true;startX=e.pageX-slider.offsetLeft;scroll=slider.scrollLeft;});slider.addEventListener('mouseleave',()=>down=false);slider.addEventListener('mouseup',()=>down=false);slider.addEventListener('mousemove',e=>{if(!down)return;e.preventDefault();const x=e.pageX-slider.offsetLeft;slider.scrollLeft=scroll-(x-startX);} );});});